# URL of the FastAPI backend.
# - Local dev: http://localhost:8000 (run `uvicorn app:app --reload` in project root)
# - Vercel deployment: set this to your Railway URL in Vercel's dashboard
API_BASE=http://localhost:8000
